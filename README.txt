Start

without docker : python manage.py runserver 0.0.0.0:8000
with docker : docker-compose up -d

APIs:
domain/api/{model}/         (get list & create)
domain/api/{model}/<pk>/    (CRDU)

Model:
storage (main)
size
color